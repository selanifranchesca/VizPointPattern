% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_contamination_map.R
\name{plot_contamination_map}
\alias{plot_contamination_map}
\title{Visualize Contamination Data on an Interactive Map and Perform Spatial Analysis
This comprehensive function filters geocoded contamination data by boundaries,
calculates Moran's I for spatial autocorrelation, and generates an interactive
Leaflet map with circle markers representing the contamination sample points.}
\usage{
plot_contamination_map(
  sf_data,
  contamination_col,
  boundary_lon_min,
  boundary_lon_max,
  boundary_lat_min,
  boundary_lat_max,
  lat_col_name = "latitude",
  lon_col_name = "longitude",
  color_palette = "YlOrRd",
  legend_title = "Contamination (Units)",
  map_title = NULL,
  map_zoom = 12,
  circle_marker_radius = 5,
  morans_k_neighbors = 8,
  morans_alternative = "greater",
  sample_id_col = "Sample.Number"
)
}
\arguments{
\item{sf_data}{An \code{sf} object (simple features data frame) with point geometries
and numeric contamination data, typically the output from \code{geocode_contamination_data()}.}

\item{contamination_col}{Character string. Name of the numeric column in \code{sf_data}
that contains the contamination values (e.g., "lead_ppb", "arsenic_mg_kg").}

\item{boundary_lon_min}{Numeric. Minimum longitude for filtering data.}

\item{boundary_lon_max}{Numeric. Maximum longitude for filtering data.}

\item{boundary_lat_min}{Numeric. Minimum latitude for filtering data.}

\item{boundary_lat_max}{Numeric. Maximum latitude for filtering data.}

\item{lat_col_name}{Character string. Name of the latitude column in \code{sf_data}
(e.g., "latitude"). Used for internal filtering.}

\item{lon_col_name}{Character string. Name of the longitude column in \code{sf_data}
(e.g., "longitude"). Used for internal filtering.}

\item{color_palette}{Character string or vector. The RColorBrewer palette name
(e.g., "YlOrRd", "Greens") or a vector of colors for the contamination gradient.
Defaults to "YlOrRd".}

\item{legend_title}{Character string. Title for the map legend (e.g., "Lead (PPB)").}

\item{map_title}{Character string. Overall title displayed on the map (optional).}

\item{map_zoom}{Integer. Initial zoom level for the Leaflet map (default: 12).}

\item{circle_marker_radius}{Numeric. Radius of the circle markers on the map (default: 5).}

\item{morans_k_neighbors}{Integer. Number of nearest neighbors for Moran's I calculation (default: 8).}

\item{morans_alternative}{Character string. Alternative hypothesis for Moran's I
("greater", "less", "two.sided", default: "greater").}

\item{sample_id_col}{Character string. Name of the column with sample IDs for labels (default: "Sample.Number").}
}
\value{
A \code{leaflet} map object. Also prints Moran's I results to the console.
}
\description{
Visualize Contamination Data on an Interactive Map and Perform Spatial Analysis
This comprehensive function filters geocoded contamination data by boundaries,
calculates Moran's I for spatial autocorrelation, and generates an interactive
Leaflet map with circle markers representing the contamination sample points.
}
